apiVersion: apps/v1
kind: Deployment
metadata:
  name: mi-app
spec:
#  nodeSelector:
  replicas: 4
  selector:
    matchLabels:
      app: mi-app
  template:
    metadata:
      labels:
        app: mi-app
    spec:
      containers:
      - name: nginx
        image: nginx
